// Centralized timing constants used by the cart removal/undo flow
export const REMOVE_ANIM_MS = 180; // how long the slide-out animation lasts
export const UNDO_WINDOW_MS = 2000; // how long the user has to undo
export const SLIDE_IN_MS = 280; // slide-in animation duration
export const TOAST_DELAY_MS = 220; // delay before starting slide-out so toast is visible
